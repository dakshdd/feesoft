import 'package:flutter/material.dart';

class FeeEntryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Fee Entry")),
      body: Center(child: Text("Fee Entry Form Here")),
    );
  }
}
