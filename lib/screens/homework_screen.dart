import 'package:flutter/material.dart';

class HomeworkScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Homework")),
      body: const Center(
        child: Text("Homework Screen Placeholder"),
      ),
    );
  }
}