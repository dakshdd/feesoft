import 'package:flutter/material.dart';

class ChatBoxScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Chat Box")),
      body: const Center(
        child: Text("Chat Box Screen Placeholder"),
      ),
    );
  }
}