import 'package:flutter/material.dart';

class CircularScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Circulars")),
      body: const Center(
        child: Text("Circular Screen Placeholder"),
      ),
    );
  }
}